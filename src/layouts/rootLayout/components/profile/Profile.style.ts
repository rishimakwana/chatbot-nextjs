import { TStyle } from '@/types'

export const style: TStyle = {
  root: {
    flexFlow: 'row',
    alignItems: 'center',
    gap: 2
  },
  avatar: {
    height: 48,
    width: 48,
    borderRadius: 4,
  },
}
