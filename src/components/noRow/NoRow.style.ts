import { TStyle } from '@/types'

export const style: TStyle = {
  root: {
    width: 1,
    alignItems: 'center',
    gap: 2,
    textAlign: 'center',
    mb: 1,
  },
  subtitle: {
    opacity: 0.8,
  },
}
